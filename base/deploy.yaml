apiVersion: apps/v1
kind: Deployment
metadata:
  name: mywebapp
  namespace: default
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: mycontainer
        image: "kustom-nginx:1"
        ports:
        - containerPort: 80
